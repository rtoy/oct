;;; -*- Mode: lisp -*-

(mk:defsystem qd
  :source-pathname (make-pathname :directory (pathname-directory *load-pathname*))
  :components
  ((:file "qd-package")
   (:file "qd")
   (:file "qd-const")
   (:file "qd-fun"))
)